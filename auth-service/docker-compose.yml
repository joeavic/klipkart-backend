version: '3.3'

services:
  auth-service:
    build: .
    container_name: auth-service
    ports:
      - "8082:8080"
    networks:
      - my-network

volumes:
  pgdata:

networks:
  my-network:
    external: true


